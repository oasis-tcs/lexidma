<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
                         "http://www.docbook.org/xml/4.5/docbookx.dtd" [
                         <!ENTITY % xinclude SYSTEM "../../../docbook/xinclude.mod" >
                         %xinclude;
                         <!ENTITY % local.common.attrib "xml:base CDATA #IMPLIED" >
]>

<section id="annotation_itemMarker">
  <title><code>itemMarker</code></title>
  <para>Marks up a substring other than the headword inside an example, inside an example
    translation or inside a definition. An application can use the inline markup to highlight
    collocates or constituents. Example: <xref linkend="ex22"/>.</para>

  <itemizedlist>
    <title>Child of</title>
    <listitem>
      <para><literal><olink targetptr="annotation_definition">definition</olink></literal></para>
    </listitem>
    <listitem>
      <para><literal><olink targetptr="annotation_example">example</olink></literal></para>
    </listitem>
    <listitem>
      <para><literal><olink targetptr="annotation_exampleTranslation"
          >exampleTranslation</olink></literal></para>
    </listitem>
  </itemizedlist>

  <itemizedlist>
    <title>Contents</title>
    <listitem>
      <para><literal>id</literal>
        <glossterm>optional</glossterm> (zero or one). A unique identifier of the marker.
        Markers which have idenfitiers are capable of being involved in relations created with the
        <olink targetptr="linking">Linking module</olink>.</para>
    </listitem>
    <listitem>
      <para><literal>lemma</literal>
        <glossterm>optional</glossterm> (zero or one). Non-empty string. The lemmatized form of the
        collocate. An application can use it to provide a clickable link for the user to search for
        the lemma in the rest of the lexicographic resource or on the web. (If you want to link the
        collocate explicitly to a specific entry or to a specific sense in your lexicographic
        resource, or even in an external lexicographic resource, you can use the Linking Module for
        that.)</para>
    </listitem>
    <listitem>
      <para><literal>role</literal>
        <glossterm>optional</glossterm> (zero, one or more). Non-empty string. Can be used to
        communicate facts about the role of the item in the sentence, for example its syntactic role
        (subject, direct object etc.), its semantic role (agent, affected etc) or its semantic type
        (human, institution etc.) The <code><olink targetptr="annotation_itemMarkerRoleTag">itemMarkerRoleTag</olink></code> object
        type can be used to explain and/or constrain the item types that are allowed to appear in
        the lexicographic resource.</para>
    </listitem>
  </itemizedlist>

  <example>
    <title>XML</title>
    <programlisting>
&lt;itemMarker id="..." lemma="..."&gt;
    ...
    &lt;role tag="..."/&gt;
&lt;/itemMarker&gt;
    </programlisting>
  </example>

  <example>
    <title>JSON</title>
    <programlisting>
{
    "id": "...",
    "startIndex": ...,
    "endIndex": ...,
    "lemma": "...",
    "roles": ["..."]
}
    </programlisting>
  </example>
  <example>
      <title>RDF</title>
      <programlisting>
&lt;#definition&gt; dmlex:itemMarker [
  dmlex:id "..." ;
  dmlex:startIndex 0 ;
  dmlex:endIndex 0 ;
  dmlex:lemma "..." ;
  dmlex:role ... ] .
      </programlisting>
  </example>
  <example>
    <title>SQL</title>
    <graphic fileref="modules/annotation/databaseDiagrams/itemMarker.svg" contentwidth="16cm"/>
  </example>

</section>
