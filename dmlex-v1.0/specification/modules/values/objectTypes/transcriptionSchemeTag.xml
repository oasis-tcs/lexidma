<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
                         "http://www.docbook.org/xml/4.5/docbookx.dtd" [
                         <!ENTITY % xinclude SYSTEM "../../../docbook/xinclude.mod" >
                         %xinclude;
                         <!ENTITY % local.common.attrib "xml:base CDATA #IMPLIED" >
]>

<section id="values_transcriptionSchemeTag">

  <title>
    <code>transcriptionSchemeTag</code>
  </title>
  <para>Represents one (of many) possible values for
    <code>scheme</code> of <code><olink targetptr="core_transcription">transcription</olink></code>.
  </para>

  <itemizedlist>
    <title>Property of</title>
    <listitem>
      <para><literal><olink targetptr="core_lexicographicResource">lexicographicResource</olink></literal></para>
    </listitem>
  </itemizedlist>

  <itemizedlist>
    <title>Properties</title>
    <listitem>
      <para><literal>tag</literal>
        <glossterm>required</glossterm> (exactly one). An IETF language tag.</para>
      <formalpara>
        <title>Uniqueness constraint</title>
        <para>
          No two instances of <literal>transcriptionSchemeTag</literal>
          in the same <literal>lexicographicResource</literal>
          can have the same <literal>tag</literal>.
        </para>
      </formalpara> 
    </listitem>
    <listitem>
      <para><literal>description</literal>
        <glossterm>optional</glossterm> (zero or one). Non-empty string.
        A human-readable description of what the tag means.</para>
    </listitem>

    <listitem>
      <para><literal>forHeadwords</literal>
        <glossterm>optional</glossterm> (zero or one). Boolean. If present, indicates whether this tag is
        intended to be used on the headword
        side of the lexicographic resource: inside a <literal>pronunciation</literal> of <literal><olink targetptr="core_entry">entry</olink></literal>.</para>
    </listitem>
    <listitem>
      <para><literal>forTranslations</literal>
        <glossterm>optional</glossterm> (zero or one). Boolean. If present, indicates whether this tag is
        intended to be used on the translation side of the lexicographic resource:
        inside a <literal>pronunciation</literal> of <literal><olink targetptr="xlingual_headwordTranslation">headwordTranslation</olink></literal>.</para>
    </listitem>

    <listitem>
      <para><literal>forLanguage</literal>
        <glossterm>optional</glossterm> (zero or more). If present, says that if this tag is
        being used inside a <code><olink targetptr="xlingual_headwordTranslation">headwordTranslation</olink></code> object,
        then it is intended to be used only inside a <code>headwordTranslation</code> object labelled with this language.</para>
    </listitem>
    
  </itemizedlist>
  
  <itemizedlist>
    <title>Comment</title>
    <listitem>
      <para>The <code>transcriptionSchemeTag</code> does not have a <code>sameAs</code> property
      because the tag itself - which is an IETF language tag - defines fully what the tag means.</para>
    </listitem>
  </itemizedlist>
  

  <example>
    <title>XML</title>
    <programlisting>
&lt;transcriptionSchemeTag tag="..." forHeadwords="true" forTranslations="true"&gt;
    &lt;description&gt;...&lt;/description&gt;
    &lt;forLanguage langCode="..."/&gt;
&lt;/transcriptionSchemeTag&gt;
  </programlisting>
  </example>
  
  <example>
    <title>JSON</title>
    <programlisting>
{
    "tag": "...",
    "description": "...",
    "forHeadwords": true,
    "forTranslations": true,
    "forLanguages": ["..."]
}
    </programlisting>
  </example>
  
  <example>
    <title>RDF</title>
    <programlisting>
&lt;lexicographicResource&gt; dmlex:transcriptionSchemeTag [
  a dmlex:PartOfSpeechTag ;
  dmlex:tag "..." ;
  dmlex:description "..." ;
  dmlex:forHeadwords "..." ;
  dmlex:forTranslations "..." ;
  dmlex:forLanguage "..." ] .
    </programlisting>
  </example>
  
  <example>
    <title>Relational database</title>
    <graphic fileref="modules/values/databaseDiagrams/transcriptionSchemeTag.svg" contentwidth="16cm"/>
  </example>
  
</section>
