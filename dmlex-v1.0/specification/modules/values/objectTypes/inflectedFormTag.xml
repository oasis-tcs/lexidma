<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
                         "http://www.docbook.org/xml/4.5/docbookx.dtd" [
                         <!ENTITY % xinclude SYSTEM "../../../docbook/xinclude.mod" >
                         %xinclude;
                         <!ENTITY % local.common.attrib "xml:base CDATA #IMPLIED" >
]>

<section id="values_inflectedFormTag">

  <title>
    <code>inflectedFormTag</code>
  </title>
  <para>Represents one (of many) possible values for
    <code>tag</code> of <code><olink targetptr="core_inflectedForm">inflectedForm</olink></code>.
    Example: <xref linkend="ex05"/>.
  </para>

  <itemizedlist>
    <title>Child of</title>
    <listitem>
      <para><literal><olink targetptr="core_lexicographicResource">lexicographicResource</olink></literal></para>
    </listitem>
  </itemizedlist>

  <itemizedlist>
    <title>Contents</title>
    <listitem>
      <para><literal>tag</literal>
        <glossterm>required</glossterm> (exactly one). Non-empty string. An abbreviation, a code or
        some other string of text.</para>
    </listitem>
    <listitem>
      <para><literal>description</literal>
        <glossterm>optional</glossterm> (zero or one). Non-empty string.
        A human-readable description of what the tag means.</para>
    </listitem>

    <listitem>
      <para><literal>forHeadwords</literal>
        <glossterm>optional</glossterm> (zero ore one). Boolean. If present, indicates whether this tag is
        intended to be used on the headword
        side of the lexicographic resource: as <literal>inflectedForm</literal> of <literal><olink targetptr="core_entry">entry</olink></literal>.</para>
    </listitem>
    <listitem>
      <para><literal>forTranslations</literal>
        <glossterm>optional</glossterm> (zero ore one). Boolean. If present, indicates whether this tag is
        intended to be used on the translation side of the lexicographic resource:
        as <literal>headwordTranslation</literal> of <literal><olink targetptr="xlingual_headwordTranslation">headwordTranslation</olink></literal>.</para>
    </listitem>

    <listitem>
      <para><literal>forLanguage</literal>
        <glossterm>optional</glossterm> (zero, one or more). If present, says that:</para>
      <itemizedlist>
        <listitem>
          <para>If this tag is
            being used inside a <code><olink targetptr="xlingual_headwordTranslation">headwordTranslation</olink></code> object,
            then it is intended to be used only inside a <code>headwordTranslation</code> object labelled with this language.</para>
        </listitem>
      </itemizedlist>
    </listitem>

    <listitem>
      <para><literal>forPartOfSpeech</literal>
        <glossterm>optional</glossterm> (zero, one or more). If present, says that:</para>
      <itemizedlist>
        <listitem>
          <para>If this tag is used as a <code>inflectedForm</code> of a <code>headwordTranslation</code>, then
            the <code>headwordTranslation</code> must have this part of
            speech.</para>
        </listitem>
        <listitem>
          <para>If this tag is used as a <code>inflectedForm</code> of an <code>entry</code>, then 
            the <code>entry</code> must have this part of speech.</para>
        </listitem>
      </itemizedlist>
    </listitem>
    <listitem>
      <para><literal><olink targetptr="values_sameAs">sameAs</olink></literal>
        <glossterm>optional</glossterm> (zero, one or more).</para>
    </listitem>
  </itemizedlist>

  <example>
    <title>XML</title>
    <programlisting>
&lt;inflectedFormTag tag="..." forHeadwords="true" forTranslations="true"&gt;
    &lt;description&gt;...&lt;/description&gt;
    &lt;forLanguage langCode="..."/&gt;
    &lt;forPartOfSpeech tag="..."/&gt;
    &lt;sameAs.../&gt;
&lt;/tag&gt;
  </programlisting>
  </example>
  
  <example>
    <title>JSON</title>
    <programlisting>
{
    "tag": "...",
    "description": "...",
    "forHeadwords": true,
    "forTranslations": true,
    "forLanguages": ["..."]
    "forPartsOfSpeech": ["..."],
    "sameAs": [...]
}
    </programlisting>
  </example>
  
  <example>
    <title>RDF</title>
    <programlisting>
&lt;lexicographicResource&gt; dmlex:inflectedFormTag [
  a dmlex:InflectedFormTag ;
  dmlex:tag "..." ;
  dmlex:description "..." ;
  dmlex:forHeadwods "..." ;
  dmlex:forTranslations "..." ;
  dmlex:forLanguage "..." .
  dmlex:forPartOfSpeech "..." ;
  dmlex:sameAs ... ] .
    </programlisting>
  </example>
  
  <example>
    <title>Relational database</title>
    <graphic fileref="modules/values/databaseDiagrams/inflectedFormTag.svg" contentwidth="16cm"/>
  </example>
  
</section>
