<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
                         "http://www.docbook.org/xml/4.5/docbookx.dtd" [
                         <!ENTITY % xinclude SYSTEM "../../../docbook/xinclude.mod" >
                         %xinclude;
                         <!ENTITY % local.common.attrib "xml:base CDATA #IMPLIED" >
]>

<section>
  <title><literal>SubsenseRelation</literal> relation type</title>
  <para>
    Represents the fact that one sense (the subordinate sense) should be
    treated as a subsense of another sense (the subordinate). Both
    senses belong to the same entry.
  </para>
  <para>
    Participants:
  </para>
  <itemizedlist spacing="compact">
    <listitem>
      <para>
        the superordinate <literal>Sense</literal> (exactly one)
      </para>
    </listitem>
    <listitem>
      <para>
        the subordinate <literal>Sense</literal> (exactly one)
      </para>
    </listitem>
  </itemizedlist>
</section>
