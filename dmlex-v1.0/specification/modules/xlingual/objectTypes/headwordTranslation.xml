<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
                         "http://www.docbook.org/xml/4.5/docbookx.dtd" [
                         <!ENTITY % xinclude SYSTEM "../../../docbook/xinclude.mod" >
                         %xinclude;
                         <!ENTITY % local.common.attrib "xml:base CDATA #IMPLIED" >
]>

<section id="xlingual_headwordTranslation">
  <title>
    <code>headwordTranslation</code>
  </title>
  <para>Represents one of possibly multiple translations of a headword. Examples: <xref
      linkend="ex08"/>, <xref linkend="ex11"/>.</para>

  <itemizedlist>
    <title>Child of</title>
    <listitem>
      <para><literal><olink targetptr="xlingual_sense">sense</olink></literal></para>
    </listitem>
  </itemizedlist>

  <itemizedlist>
    <title>Contents</title>
    <listitem>
      <para><literal>text</literal>
        <glossterm>required</glossterm> (exactly one). Non-empty string.</para>
    </listitem>
    <listitem>
      <para><literal>language</literal>
        <glossterm>optional</glossterm> (zero or one) if only one translation language exists in thelexicographic resource,
        <glossterm>required</glossterm> (one or more) otherwise.
        IETF language tag. Indicates the language of
        this translation. The <code><olink targetptr="xlingual_translation"
            >translationLanguage</olink></code> datatype can be used to explain the meaning of the
        language codes that appear here and/or to constrain which language codes are allowed.</para>
    </listitem>
    <listitem>
      <para><literal>listingOrder</literal>
        <glossterm>required</glossterm> (exactly one). Number. The position of this translation
        among other translations of the same sense in the same language. Can be implicit from the
        serialization.</para>
    </listitem>
    <listitem>
      <para><literal><olink targetptr="xlingual_partOfSpeech">partOfSpeech</olink></literal>
        <glossterm>optional</glossterm> (zero, one or more). </para>
    </listitem>
    <listitem>
        <para><literal><olink targetptr="xlingual_label">label</olink></literal>
        <glossterm>optional</glossterm> (zero, one or more). </para>
    </listitem>
    <listitem>
        <para><literal><olink targetptr="xlingual_pronunciation">pronunciation</olink></literal>
        <glossterm>optional</glossterm> (zero, one or more). </para>
    </listitem>
    <listitem>
        <para><literal><olink targetptr="xlingual_inflectedForm">inflectedForm</olink></literal>
        <glossterm>optional</glossterm> (zero, one or more). </para>
    </listitem>
  </itemizedlist>

  <example>
    <title>XML</title>
    <programlisting>
&lt;headwordTranslation language="..."&gt;
    &lt;text&gt;...&lt;/text&gt;
    &lt;partOfSpeech.../&gt;
    &lt;label.../&gt;
    &lt;pronunciation.../&gt;
    &lt;inflectedForm.../&gt;
&lt;/headwordTranslation&gt;
    </programlisting>
  </example>
  
  <example>
    <title>JSON</title>
    <programlisting>
{
    "language": "...",
    "text": "...",
    "partsOfSpeech": [...],
    "labels": [...],
    "pronunciations": [...],
    "inflectedForms": [...]
}
    </programlisting>
  </example>
  
  <example>
    <title>Relational database</title>
    <programlisting>
create table headwordTranslations (
    senseID int foreign key references senses(id),
    language nvarchar(10) foreign key references translationLanguage(langCode),
    text nvarchar(255),
    listingOrder int,
    id int primary key
);
alter table partsOfSpeech (
    add headwordTranslationID int foreign key references headwordTranslations(id)
);
alter table labels (
    add headwordTranslationID int foreign key references headwordTranslations(id)
);
alter table pronunciations (
    add headwordTranslationID int foreign key references headwordTranslations(id)
);
alter table inflectedForms (
    add headwordTranslationID int foreign key references headwordTranslations(id)
)
    </programlisting>
  </example>

</section>
