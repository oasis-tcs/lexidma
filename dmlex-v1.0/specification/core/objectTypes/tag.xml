<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
                         "http://www.docbook.org/xml/4.5/docbookx.dtd" [
                         <!ENTITY % xinclude SYSTEM "../../docbook/xinclude.mod" >
                         %xinclude;
                         <!ENTITY % local.common.attrib "xml:base CDATA #IMPLIED" >
]>

<section id="core_tag">

  <title>
    <code>tag</code>
  </title>
  <para>Represents one (of many) possible values for
    <code>value</code> of <code><olink targetptr="core_partOfSpeech">partOfSpeech</olink></code>,
    <code>inflectedTag</code> of <code><olink targetptr="core_inflectedForm">inflectedForm</olink></code>,
    <code>definitionType</code> of <code><olink targetptr="core_definition">definition</olink></code>,
    <code>value</code> of <code><olink targetptr="core_label">label</olink></code> or
    <code>sourceIndentity</code> of <code><olink targetptr="core_example">example</olink></code>.
    Example: <xref linkend="ex05"/>.
  </para>

  <itemizedlist>
    <title>Child of</title>
    <listitem>
      <para><literal><olink targetptr="core_lexicographicResource">lexicographicResource</olink></literal></para>
    </listitem>
  </itemizedlist>

  <itemizedlist>
    <title>Contents</title>
    <listitem>
      <para><literal>value</literal>
        <glossterm>required</glossterm> (exactly one). Non-empty string.
        An abbreviation, a code or some other string of text.</para>
    </listitem>
    <listitem>
      <para><literal>description</literal>
        <glossterm>optional</glossterm> (zero or one). Non-empty string.
        A human-readable description of what the tag means.</para>
    </listitem>
    <listitem>
      <para><literal>target</literal>
        <glossterm>optional</glossterm> (zero, one or more).
        Indicates how the value of this tag can be used in this lexicographic resource.
        One of:
        <itemizedlist>
          <listitem>
            <para><literal>partOfSpeech</literal> The tag value can be used in
              <code>value</code> of <olink targetptr="core_partOfSpeech"
                ><code>partOfSpeech</code></olink>.</para>
          </listitem>
          <listitem>
            <para><literal>inflectedTag</literal> The tag value can be used in
              <code>inflectedTag</code> of <olink targetptr="core_inflectedForm"
                ><code>inflectedForm</code></olink>.</para>
          </listitem>
          <listitem>
            <para><literal>definitionType</literal> The tag value can be used in
              <code>definitionType</code> of <olink targetptr="core_definition"
                ><code>definition</code></olink>.</para>
          </listitem>
          <listitem>
            <para><literal>label</literal> The tag value can be used in
              <code>value</code> of <olink targetptr="core_label"
                ><code>label</code></olink>.</para>
          </listitem>
          <listitem>
            <para><literal>sourceIndentity</literal> The tag value can be used in
              <code>sourceIdentity</code> of <olink targetptr="core_example"
                ><code>example</code></olink>.</para>
          </listitem>
        </itemizedlist>
        If omitted, then all.
      </para>
    </listitem>
    <listitem>
      <para><literal>partOfSpeechConstraint</literal>
        <glossterm>optional</glossterm> (zero, one or more). If present, says that this tag
        is only intended to be used inside entries that are labelled with this part of speech.
        This can be used to constrain that, for example, only nouns and adjectives can have plurals
        but other parts of speech cannot.</para>
    </listitem>
    <listitem>
      <para><literal><olink targetptr="core_sameAs">sameAs</olink></literal>
        <glossterm>optional</glossterm> (zero, one or more).</para>
    </listitem>
  </itemizedlist>

  <itemizedlist>
    <title>Comments</title>
    <listitem>
      <para>
        In a lexicographic resource, <code>tag</code> objects can used to define
        controlled vocabularies (inventories of part-of-speech tags, labels etc.) and constrains
        on where they are expected to be used (e.g. part-of-speech tags are expected to be used
        as <code>value</code> of <code>partOfSpeech</code> objects).
      </para>
      <para>
        Enforcing these constraints in an implementation of DMLex, for example as business rules 
        in a dictionary-writing system, is <glossterm>optional</glossterm>: an implementation of
        DMLex is compliant regardless of whether it
        enforces the constraints defined by <code>tag</code> objects or not.
      </para>
    </listitem>
  </itemizedlist>
  
  <example>
    <title>XML</title>
    <programlisting>
&lt;tag value="..."&gt;
    &lt;description&gt;...&lt;/description&gt;
    &lt;target value="..."/&gt;
    &lt;partOfSpeechConstraint value="..."/&gt;
    &lt;sameAs.../&gt;
&lt;/tag&gt;
  </programlisting>
  </example>
  
  <example>
    <title>JSON</title>
    <programlisting>
{
    "value": "...",
    "description": "...",
    "targets": ["..."],
    "partOfSpeechConstraints": ["..."],
    "sameAs": [...]
}
    </programlisting>
  </example>
  
  <example>
    <title>RDF</title>
    <programlisting>
&lt;entry&gt; dmlex:tag [
  a dmlex:Tag ;
  dmlex:description "..." ;
  dmlex:target "..." ;
  dmlex:partOfSpeechConstraint "..." ;
  dmlex:sameAs ... ] .
    </programlisting>
  </example>
  
  <example>
    <title>Relational database</title>
    <graphic fileref="core/databaseDiagrams/tag.svg" contentwidth="16cm"/>
    <para>The <code>partOfSpeechConstraints</code> column is a comma-delimited list of part-of-speech labels.</para>
  </example>
  
</section>
